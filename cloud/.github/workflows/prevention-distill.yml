name: coo-prevention-distill
on:
  schedule:
    - cron: "0 20 * * 0"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_runner.yml
    with:
      routine_id: prevention-distill
      tier: A
    secrets: inherit
