name: coo-audit-chain-verify
on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_runner.yml
    with:
      routine_id: audit-chain-verify
      tier: A
    secrets: inherit
