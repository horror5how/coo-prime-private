name: coo-deploy-health
on:
  schedule:
    - cron: "0 12 * * *"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_runner.yml
    with:
      routine_id: deploy-health
      tier: B
    secrets: inherit
