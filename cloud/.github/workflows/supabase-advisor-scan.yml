name: coo-supabase-advisor-scan
on:
  schedule:
    - cron: "0 6 * * 3"
  workflow_dispatch:
jobs:
  run:
    uses: ./.github/workflows/_runner.yml
    with:
      routine_id: supabase-advisor-scan
      tier: C
    secrets: inherit
